import { ImgHightlightDirective } from './img-hightlight.directive';

describe('ImgHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
